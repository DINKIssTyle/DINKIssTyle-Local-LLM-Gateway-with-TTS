# Bundled skills

This directory is reserved for read-only skills shipped with the application.
Each enabled skill will live in its own directory and contain a `SKILL.md`.

Bundled skills are not loaded yet. See `PARSING_AND_SKILLS_PLAN.md` for the
validation, permission, and rollout plan. User skills belong in the writable
`skills/user` directory shown in the server window.
